import { combineReducers } from 'redux';

import course from './course';

export default combineReducers({
    course
});


// uso para combinar reducers (como só tem um no projeto, então só tem um aqui)